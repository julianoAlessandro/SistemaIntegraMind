/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package com.mycompany.sistemaintegramind.util.Utilitarios;


import javax.swing.JTable;
import javax.swing.JTextField;
import javax.swing.table.DefaultTableModel;



public class CalculosFinanceiros {
    /*
     public static  double recalcularTotalVenda(JTable tblVenda, JTextField txtTotal, Vendas vendaAtual ) {
        double total = 0;

        DefaultTableModel model = (DefaultTableModel) tblVenda.getModel();

        for (int i = 0; i < model.getRowCount(); i++) {

            double subtotal = Double.parseDouble(model.getValueAt(i, 6).toString());

            total += subtotal;
        }

        EstilizarFormatação.formatarestilototalvendas(total, txtTotal);
        vendaAtual.setTotal(total);
        return total;
    }

*/
}
